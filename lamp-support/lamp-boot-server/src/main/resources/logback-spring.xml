<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- defaults-biz-prod.xml 文件位于lamp-cloud或lamp-boot 的 lamp-public/lamp-common/src/main/resources -->
    <include resource="defaults-biz-prod.xml"/>

    <!-- 可以在这里自定义规则 -->
    <logger name="top.tangyh.lamp.generator.controller" additivity="true" level="${log.level.controller}">
        <appender-ref ref="ASYNC_CONTROLLER_APPENDER"/>
    </logger>
    <logger name="top.tangyh.lamp.generator.service" additivity="true" level="${log.level.service}">
        <appender-ref ref="ASYNC_SERVICE_APPENDER"/>
    </logger>
    <logger name="top.tangyh.lamp.generator.mapper" additivity="false" level="${log.level.mapper}">
        <appender-ref ref="ASYNC_MAPPER_APPENDER"/>
    </logger>
</configuration>
